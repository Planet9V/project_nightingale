import { PineconeConnector } from '../connectors/PineconeConnector';

export class PineconeManager extends PineconeConnector {
  constructor(config: any) {
    super(config);
  }

  // Additional manager methods can be added here
}